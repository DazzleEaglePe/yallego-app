<?xml version="1.0" encoding="utf-8"?>
<!--
  Fusionado únicamente en compilaciones de depuración. `API_BASE_URL_DEBUG`
  apunta a un backend local (emulador o IP de LAN) sin TLS; la compilación de
  publicación no incluye este archivo y exige HTTPS (docs/07_SEGURIDAD_AUTH.md §7.3).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:networkSecurityConfig="@xml/network_security_config_debug"
        tools:ignore="GoogleAppIndexingWarning"
        xmlns:tools="http://schemas.android.com/tools" />
</manifest>
