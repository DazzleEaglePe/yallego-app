services:
  api:
    ports:
      - '127.0.0.1:${API_HOST_PORT:-3101}:3001'

  dashboard:
    ports:
      - '127.0.0.1:${DASHBOARD_HOST_PORT:-3100}:3000'

  prometheus:
    ports:
      - '127.0.0.1:${PROMETHEUS_HOST_PORT:-9090}:9090'
